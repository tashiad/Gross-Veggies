language: node_js
node_js:
  - "node"
cache:
  npm: true
  directories:
    - ~/.cache
install:
  - npm ci
  - npm run build
script:
  - npm start & wait-on http://localhost:3000
  - $(npm bin)/cypress run
